# Minimap icons

Marks a location with an icon on the leaderboard minimap and as a distance-faded marker in the world.

## How-to

Give a Model or a BasePart a "minimap_icon" tag and an "icon" string attribute set to an image asset id (e.g. rbxassetid://10223378964). Models use their pivot as the marker position, parts use their position.
